# txt2phrases
Extract keyphrases from documents using open source libraries

This repo was originally bundled as a subproject of `semanticClimate` `encyclopedia` project. This refactoring will make it standalone and the new version will be uploadedto PyPI.
